# TC server example

Use the TC server example for topology, labels, DDOP upload/activation, and
process data workflows.

Look for:

- `examples/tc_server_demo.rs`
- the `TcServer` plugin on the session facade (see
  [The session facade](../guide/session-facade.md))

Validate section/boom/channel configuration before advertising it.
